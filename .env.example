# --- SprigaAPI integration ---------------------------------------------------
# Base URL of the SprigaAPI backend. Required — the app runs on live data.
NEXT_PUBLIC_API_URL=https://spriga-api.essa.sx
